apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: sante-medical
  labels:
    app: frontend
    tier: presentation
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: frontend
  sessionAffinity: None
